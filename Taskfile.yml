# github.com/go-task/task

dev:
  cmds:
    - go run ./cli.go ./circleSet.go  ./circleManager.go --dbHost 127.0.0.1 --dbPort 3306 --dbName circle --dbUser root --dbPassword jjgo --circleID 1 --rootPath example

test:
  cmds:
    - ^clean
    - go test *.go -v

clean:
  cmds:
    - rm -rf example/admin/*
    - rm -rf example/controllers/*
    - rm -rf example/models/*
    - rm -rf example/requests/*
    - rm -rf example/responses/*
    - rm -rf example/routers/*